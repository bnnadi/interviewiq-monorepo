version: '3.8'

services:
  frontend:
    ports:
      - "3001:3000"  # Use port 3001 instead of 3000
  
  backend:
    ports:
      - "8000:8000"  # Keep backend on 8000
  
  ollama:
    ports:
      - "11434:11434"  # Keep ollama on 11434 